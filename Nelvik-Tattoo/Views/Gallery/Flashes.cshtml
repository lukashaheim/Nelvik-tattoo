@model IEnumerable<GalleryDesign>

@{
    ViewData["Title"] = "Flash Tattoos";
}

@section Styles {
    <link rel="stylesheet" href="~/css/gallery.css" asp-append-version="true" />
}

<section class="gallery-page container flash-page">
    <h1>@ViewData["Title"]</h1>

    <p>Click on a flash design to view details and book the design.</p>

    <div class="gallery-grid">
        @foreach (var item in Model.Where(m => m.IsFlash))
        {
            <a asp-controller="Gallery"
               asp-action="Details"
               asp-route-id="@item.Id"
               class="gallery-item">

                <img src="@item.ImagePath"
                     alt="@item.Title"
                     class="gallery-img" />

            </a>
        }
    </div>
</section>